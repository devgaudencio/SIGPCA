document.addEventListener('DOMContentLoaded', function() {
  if (document.getElementById('dashboard')) {
    // Adicione aqui apenas o código real do dashboard, se houver.
    // Removido: animação de números, simulação de atividades, exemplos.
  }
}); 